{
  "name": "madhavm-com",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.3.0",
    "npm": ">=9.6.5"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "check": "astro check && astro build",
    "preview": "astro preview",
    "verify": "./scripts/verify.sh",
    "check:live": "./scripts/check-live.sh",
    "new:post": "node ./scripts/new-post.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --project-name madhavm --branch main",
    "deploy:preview": "npm run build && wrangler pages deploy dist --project-name madhavm"
  },
  "dependencies": {
    "astro": "^5.1.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.7.3",
    "wrangler": "^4.51.1"
  }
}
