---
import BaseLayout from '../layouts/BaseLayout.astro';
---
<BaseLayout
  title="Madhav Mathur | madhavm.com"
  description="Madhav Mathur: Tech Lead for API platform and integrations in New York City."
>
  <section class="hero">
    <figure class="profile-photo" aria-label="Portrait">
      <img src="/images/profile.jpg" alt="Portrait of Madhav Mathur" loading="eager" decoding="async" />
    </figure>
    <div class="hero-copy">
      <p class="eyebrow">New York City</p>
      <h1>Tech Lead working on API platform and integration systems.</h1>
      <p class="lede">I lead API Platform and Integrations at TheGuarantors.</p>
      <p class="body-copy">Previously: Amazon (payments and chargebacks), Bentley Systems (platform tooling).</p>
      <p class="body-copy">Education: Cornell M.Eng. Computer Science, UC San Diego B.S. Computer Science.</p>
      <p class="body-copy">
        Links:
        <a class="inline-link" href="https://github.com/madhavMathur" target="_blank" rel="noreferrer">GitHub</a>,
        <a class="inline-link" href="https://www.linkedin.com/in/mathurmadhav/" target="_blank" rel="noreferrer">LinkedIn</a>,
        <a class="inline-link" href="/blog">Writing</a>
      </p>
    </div>
  </section>

  <section class="section" aria-label="Interests">
    <p class="section-kicker">Interests</p>
    <ul class="facts-list">
      <li><strong>Tech:</strong> API contracts, backend reliability, and platform architecture.</li>
      <li><strong>Art:</strong> visual composition, architecture, and writing craft.</li>
      <li><strong>People:</strong> communication patterns and team operating systems.</li>
    </ul>
  </section>

  <section class="section" id="contact" aria-label="Contact">
    <p class="section-kicker">Contact</p>
    <p class="body-copy">
      <a id="contact-mailto" class="inline-link" href="mailto:madhavmathur@outlook.com?subject=Website%20inquiry%20from%20madhavm.com">Email</a>
    </p>
  </section>
</BaseLayout>

<script>
  const contactLink = document.getElementById('contact-mailto');

  if (contactLink instanceof HTMLAnchorElement) {
    const source = window.location.href;
    const referrer = document.referrer || 'direct';
    const timezone = Intl.DateTimeFormat().resolvedOptions().timeZone || 'unknown';
    const timestamp = new Date().toISOString();

    const subject = 'Website inquiry from madhavm.com';
    const body = [
      'Hi Madhav,',
      '',
      'I found you through your website and wanted to reach out.',
      '',
      '--- Context ---',
      `Source URL: ${source}`,
      `Referrer: ${referrer}`,
      `Timezone: ${timezone}`,
      `Timestamp: ${timestamp}`,
    ].join('\\n');

    contactLink.href = `mailto:madhavmathur@outlook.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
  }
</script>
