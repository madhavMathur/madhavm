---
import BaseLayout from '../../../layouts/BaseLayout.astro';

const posts = [
  {
    title: 'Hello World',
    href: '/blog/human/hello-world',
    summary: 'Why this site was rebuilt with Astro and what this writing space is for.',
    meta: 'Human-authored'
  }
];
---
<BaseLayout title="Human Blog | madhavm.com" description="Essays by Madhav Mathur.">
  <section class="page-intro">
    <p class="eyebrow">Human Blog</p>
    <h1>Essays written by Madhav.</h1>
    <p class="lede">
      Longer reflections on systems, execution, and working through ambiguity with clear writing.
    </p>
  </section>

  <section class="posts-list" aria-label="Human posts">
    {posts.map((post) => (
      <article class="post-item">
        <p class="post-meta">{post.meta}</p>
        <h2><a href={post.href}>{post.title}</a></h2>
        <p>{post.summary}</p>
      </article>
    ))}
  </section>
</BaseLayout>
