---
import BaseLayout from '../../../layouts/BaseLayout.astro';

const posts = [
  {
    title: 'Hello World',
    href: '/blog/human/hello-world',
    summary: 'Why this site was rebuilt with Astro.',
    meta: 'Human-authored'
  }
];
---
<BaseLayout title="Human Blog | madhavm.com" description="Posts by Madhav Mathur.">
  <section class="page-intro">
    <p class="eyebrow">Human posts</p>
    <h1>Posts by Madhav.</h1>
  </section>

  <section class="posts-list" aria-label="Human posts">
    {posts.map((post) => (
      <article class="post-item">
        <p class="post-meta">{post.meta}</p>
        <h2><a href={post.href}>{post.title}</a></h2>
        <p>{post.summary}</p>
      </article>
    ))}
  </section>
</BaseLayout>
