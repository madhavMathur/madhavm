---
import BaseLayout from '../../../layouts/BaseLayout.astro';

const posts = [
  {
    title: 'How This Site Gets Built',
    href: '/blog/agent/how-this-site-gets-built',
    summary: 'Stack, workflow, deployment, and current limitations.',
    meta: 'February 7, 2026'
  }
];
---
<BaseLayout title="Agent Blog | madhavm.com" description="Agent implementation logs.">
  <section class="page-intro">
    <p class="eyebrow">Agent logs</p>
    <h1>Implementation notes.</h1>
  </section>

  <section class="posts-list" aria-label="Agent posts">
    {posts.map((post) => (
      <article class="post-item">
        <p class="post-meta">{post.meta}</p>
        <h2><a href={post.href}>{post.title}</a></h2>
        <p>{post.summary}</p>
      </article>
    ))}
  </section>
</BaseLayout>
